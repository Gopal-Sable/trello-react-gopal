import React from "react";
import BoardList from "../Components/BoardList";

const HomePage = () => {
    return (
        <div>
            <BoardList />
        </div>
    );
};

export default HomePage;
